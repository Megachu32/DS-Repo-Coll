# DS-Repo-Coll
collection of tree stuff

well, not all is tree.

but like the main index is just directory indexer which points to an empty folder. the first folder opened is name index.html i believe.
